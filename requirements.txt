# 핵심 의존성
python-dotenv>=1.0.0   # .env 파일 로드
pydantic>=2.0.0        # Master Content JSON 스키마 검증
requests>=2.31.0       # 외부 REST API 호출 (WordPress 등)
pytest>=8.0.0          # 테스트

# ---- 아래는 추후 각 클라이언트를 실제로 연동할 때 주석을 풀고 설치하세요 ----
# openai>=1.0.0                     # OpenAI API 연동 시
# anthropic>=0.40.0                 # Anthropic API 연동 시
# google-api-python-client>=2.0.0   # Google Search Console 연동 시
# google-auth>=2.0.0                # Google Search Console 인증
